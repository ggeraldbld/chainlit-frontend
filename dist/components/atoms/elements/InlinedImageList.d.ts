import { IImageElement } from '@chainlit/react-client';
interface Props {
    items: IImageElement[];
}
declare const InlinedImageList: ({ items }: Props) => JSX.Element;
export { InlinedImageList };
